<header class="fixed inset-x-0 top-0 z-30 mx-auto w-full max-w-screen-md border border-sky-600/50 py-3 shadow backdrop-blur-lg md:top-6 md:rounded-3xl lg:max-w-screen-lg">
    <div class="px-4">
        <div class="flex items-center justify-between">
            <div class="flex shrink-0">
                <a class="flex navbar-brand fs-4 logo" href="{{ path('app_homepage') }}">
					<img class="h-16 w-auto" src="/images/ProfilAuTop.png" alt="Logo">
				</a>
            </div>
            <div class="flex items-center justify-end gap-3">
                <a class="hidden items-center justify-center rounded-xl bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 transition-all duration-150 hover:bg-gray-50 sm:inline-flex" href={{path('app_register') }}>
                    Inscription
                </a>
                <div class="inline-flex items-center justify-center rounded-xl bg-sky-500 px-3 py-2 text-sm font-semibold text-white shadow-sm transition-all duration-150 hover:bg-sky-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-sky-600">
                    <a class="flex items-center" href={{path('app_login') }}>
                        {{ ux_icon('line-md:login',{ class: 'h-4 w-4 mr-2' }) }}
                        Connexion
                    </a>
                </div>
            </div>
        </div>
    </div>
</header>
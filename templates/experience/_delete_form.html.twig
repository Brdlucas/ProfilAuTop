<form method="post" class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded action=" {{ path('app_experience_delete', {'id': experience.id} ) }}" onsubmit=" return confirm('are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ experience.id) }}">
	<button class="btn">Supprimer</button>
</form>
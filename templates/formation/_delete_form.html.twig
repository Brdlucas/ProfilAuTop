<form method="post" class="bg-red-500 hover:bg-red-600 text-white font-bold py-2 px-4 rounded" action="{{ path('app_formation_delete', {'id': formation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formation.id) }}">
	<button class="btn">Delete</button>
</form>